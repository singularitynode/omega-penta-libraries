# Core async and system utilities
asyncio
aiohttp>=3.9.0

# Data handling
pydantic>=2.0
orjson>=3.9.0

# Cache / persistence
aiocache>=0.12.2
redis>=5.0.0

# Message queue (for saga conductor)
aio_pika>=9.2.1

# HTTP client for remote API / chaos testing
httpx>=0.27.0

# Database abstraction (optional)
sqlalchemy>=2.0.25
databases>=0.9.0

# Observability / tracing
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0

# Developer utilities
pytest>=8.0.0
pytest-asyncio>=0.23.0
black>=24.0.0
isort>=5.12.0
